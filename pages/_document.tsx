import { Html, Head, Main, NextScript } from "next/document";

const Document = () => (
  <Html>
    <Head />
    <body className="font-primary bg-zinc-900 text-slate-100">
      <Main />
      <NextScript />
    </body>
  </Html>
);

export default Document;
